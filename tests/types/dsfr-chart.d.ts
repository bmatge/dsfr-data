/**
 * `@gouvfr/dsfr-chart` n'expose pas de types pour ses sous-chemins (custom
 * elements Vue) : declaration minimale pour les tests qui montent le vrai
 * composant (ex. `tests/chart-map-mount.test.ts`, #651).
 */
declare module '@gouvfr/dsfr-chart/MapChart';
