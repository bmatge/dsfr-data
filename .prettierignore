dist/
app-dist/
coverage/
node_modules/
mcp-server/

# Artefacts generes (npm run build:skills) — formates par leur generateur,
# pas par prettier : sinon le fichier oscille a chaque regeneration.
apps/builder-ia/src/skills-reference.generated.ts
packages/core/custom-elements.json
mcp-server/src/skill-matching.generated.ts
**/*.html
**/*.md

# Fonds administratifs GeoJSON livres dans le paquet (node scripts/fetch-geo.mjs,
# compares octet a octet par --check) : une ligne, jamais reformates
packages/core/geo/*.json

# Export skill Claude Code (généré par build:skills, comparé octet à octet par tests/skills-export.test.ts)
skills/dsfr-data/

# Jeux de la vérification des données (tests/verif-donnees/jeux/README.md) :
# une ligne par enregistrement, un jeu se lit comme un tableau.
tests/verif-donnees/jeux/*.json

# Attendus de la troisième voix (python3 tools/oracle-py/oracle.py) : une entrée par ligne
tests/verif-donnees/attendus.json
