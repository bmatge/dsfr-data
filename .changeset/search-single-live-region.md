---
"dsfr-data": patch
---

Le lecteur d'écran n'entend plus deux comptes de résultats contradictoires : quand une liste ou un afficheur est en aval de `dsfr-data-search` (même via des facettes), seul l'afficheur annonce le compte ; le compteur `count` de search reste visible mais ne parle plus, et le mot « résultat » est accentué (#654).
