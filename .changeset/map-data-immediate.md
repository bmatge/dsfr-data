---
"dsfr-data": patch
---

`dsfr-data-chart` (cartes `map`, `map-reg`, `map-aca`, `map-monde`) : les données sont posées dès le montage du composant DSFR Chart — plus de `console.error` « Erreur lors du parsing des données data » à chaque affichage de carte (#651).
