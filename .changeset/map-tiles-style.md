---
"dsfr-data": minor
---

`dsfr-data-map` : attribut `tiles-style="muted|grey"` pour atténuer le fond de carte d'une carte thématique sans CSS de page (filtre sur le volet des tuiles de la carte, hérité par les encarts) — un fond neutre = `ign-plan` + `tiles-style="muted"` (#686).
