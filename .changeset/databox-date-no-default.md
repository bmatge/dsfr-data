---
"dsfr-data": patch
---

`dsfr-data-chart` : `databox-date` n'a plus de valeur par défaut — la DataBox affichait la date du jour (date de rendu) comme date des données quand l'attribut était omis. Sans `databox-date`, aucune date n'est rendue (#650).
