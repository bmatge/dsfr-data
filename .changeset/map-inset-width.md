---
"dsfr-data": patch
---

Encarts territoriaux (`dsfr-data-map-inset`) : nouvel attribut `width` (px, rem, `%` — `width="20%"` répartit cinq encarts sur une ligne) et largeur par défaut de 10 rem posée par la feuille injectée de la carte, que le CSS de page continue de surcharger ; plus de style inline sur l'encart (#643).
